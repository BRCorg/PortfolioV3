<section class="backup-codes-display">
    <div class="backup-container">
        <div class="success-header">
            <i class="fas fa-check-circle"></i>
            <h1>2FA activé avec succès !</h1>
            <p>Votre compte est maintenant mieux protégé</p>
        </div>

        <div class="warning-box">
            <i class="fas fa-exclamation-triangle"></i>
            <h3>Codes de récupération</h3>
            <p><strong>⚠️ IMPORTANT :</strong> Sauvegardez ces codes maintenant. Vous ne pourrez plus les voir après avoir quitté cette page !</p>
        </div>

        <div class="codes-container">
            <div class="codes-grid">
                <?php foreach ($backupCodes as $index => $code): ?>
                    <div class="backup-code-item">
                        <span class="code-number"><?= $index + 1 ?></span>
                        <code class="backup-code"><?= htmlspecialchars($code) ?></code>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>

        <div class="actions">
            <button type="button" onclick="downloadCodes()" class="btn btn-secondary">
                <i class="fas fa-download"></i> Télécharger (.txt)
            </button>
            
            <button type="button" onclick="printCodes()" class="btn btn-secondary">
                <i class="fas fa-print"></i> Imprimer
            </button>

            <button type="button" onclick="copyCodes()" class="btn btn-secondary">
                <i class="fas fa-copy"></i> Copier tout
            </button>
        </div>

        <div class="instructions">
            <h4><i class="fas fa-info-circle"></i> Comment utiliser ces codes ?</h4>
            <ul>
                <li>Chaque code ne peut être utilisé qu'<strong>une seule fois</strong></li>
                <li>Utilisez-les si vous perdez l'accès à votre application d'authentification</li>
                <li>Conservez-les dans un endroit sûr (gestionnaire de mots de passe, coffre-fort, etc.)</li>
                <li>Ne les partagez avec personne</li>
            </ul>
        </div>

        <form action="<?= $_ENV['ADMIN_SECRET_URL'] ?>/2fa/confirm-backup" method="POST" class="confirm-form">
            <input type="hidden" name="csrf_token" value="<?= \App\Middleware\AuthMiddleware::getCsrfToken() ?>">
            
            <label class="checkbox-container">
                <input type="checkbox" id="confirmSaved" required>
                <span>J'ai sauvegardé mes codes de récupération en lieu sûr</span>
            </label>

            <button type="submit" class="btn btn-primary" id="continueBtn" disabled>
                <i class="fas fa-arrow-right"></i> Continuer vers le dashboard
            </button>
        </form>
    </div>
</section>

<style>
.backup-codes-display {
    min-height: 100vh;
    background: linear-gradient(135deg, #f5f7fa 0%, #e3f2fd 100%);
    padding: 2rem;
}

.backup-container {
    max-width: 800px;
    margin: 0 auto;
    background: white;
    border-radius: 16px;
    padding: 3rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.success-header {
    text-align: center;
    margin-bottom: 2rem;
}

.success-header i {
    font-size: 5rem;
    color: #4caf50;
    margin-bottom: 1rem;
}

.success-header h1 {
    color: #4caf50;
    margin-bottom: 0.5rem;
}

.success-header p {
    color: #666;
}

.warning-box {
    background: #fff3cd;
    border-left: 4px solid #ffa726;
    padding: 1.5rem;
    border-radius: 8px;
    margin-bottom: 2rem;
}

.warning-box i {
    color: #e65100;
    font-size: 1.5rem;
    margin-right: 0.5rem;
}

.warning-box h3 {
    color: #e65100;
    margin-bottom: 0.5rem;
}

.warning-box p {
    margin: 0;
    color: #333;
}

.codes-container {
    background: #f8f9fa;
    padding: 2rem;
    border-radius: 12px;
    margin-bottom: 2rem;
}

.codes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
}

.backup-code-item {
    background: white;
    padding: 1rem;
    border-radius: 8px;
    border: 2px solid #314473;
    display: flex;
    align-items: center;
    gap: 1rem;
}

.code-number {
    background: #314473;
    color: white;
    width: 30px;
    height: 30px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 0.9rem;
    flex-shrink: 0;
}

.backup-code {
    font-family: 'Courier New', monospace;
    font-size: 1.1rem;
    color: #314473;
    font-weight: bold;
    letter-spacing: 1px;
}

.actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 2rem;
    flex-wrap: wrap;
}

.actions .btn {
    flex: 1;
    min-width: 150px;
}

.instructions {
    background: #e3f2fd;
    padding: 1.5rem;
    border-radius: 8px;
    border-left: 4px solid #2196f3;
    margin-bottom: 2rem;
}

.instructions h4 {
    color: #1976d2;
    margin-bottom: 1rem;
}

.instructions ul {
    margin: 0;
    padding-left: 1.5rem;
}

.instructions li {
    margin: 0.5rem 0;
    color: #333;
}

.confirm-form {
    text-align: center;
}

.checkbox-container {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
    cursor: pointer;
    font-size: 1.1rem;
}

.checkbox-container input[type="checkbox"] {
    width: 20px;
    height: 20px;
    cursor: pointer;
}

.btn {
    padding: 1rem 2rem;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: 0.2s;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
}

.btn-primary {
    background: #314473;
    color: white;
}

.btn-primary:hover:not(:disabled) {
    background: #293961;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(49, 68, 115, 0.3);
}

.btn-primary:disabled {
    background: #ccc;
    cursor: not-allowed;
}

.btn-secondary {
    background: #f8f9fa;
    color: #314473;
    border: 2px solid #314473;
}

.btn-secondary:hover {
    background: #314473;
    color: white;
}

@media (max-width: 768px) {
    .backup-container {
        padding: 2rem 1.5rem;
    }

    .codes-grid {
        grid-template-columns: 1fr;
    }

    .actions {
        flex-direction: column;
    }

    .actions .btn {
        width: 100%;
    }
}

@media print {
    .backup-codes-display {
        background: white;
    }

    .actions,
    .confirm-form {
        display: none;
    }
}
</style>

<script>
// Activer le bouton Continue quand la checkbox est cochée
document.getElementById('confirmSaved').addEventListener('change', function() {
    document.getElementById('continueBtn').disabled = !this.checked;
});

// Copier tous les codes
function copyCodes() {
    const codes = <?= json_encode($backupCodes) ?>;
    const text = codes.join('\n');
    
    navigator.clipboard.writeText(text).then(() => {
        alert('✅ Codes copiés dans le presse-papiers !');
    }).catch(() => {
        alert('❌ Erreur lors de la copie');
    });
}

// Télécharger les codes
function downloadCodes() {
    const codes = <?= json_encode($backupCodes) ?>;
    const text = `CODES DE RÉCUPÉRATION 2FA
Portfolio - ${new Date().toLocaleString()}

⚠️ IMPORTANT : Ces codes ne peuvent être utilisés qu'une seule fois !
Conservez-les en lieu sûr.

${codes.map((code, i) => `${i + 1}. ${code}`).join('\n')}

---
Généré le : ${new Date().toLocaleString()}
`;

    const blob = new Blob([text], { type: 'text/plain' });
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `2fa-backup-codes-${Date.now()}.txt`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    window.URL.revokeObjectURL(url);
}

// Imprimer les codes
function printCodes() {
    window.print();
}
</script>
